import React from 'react';
import PropTypes from 'prop-types';

const RadarChart = ({ selectedPlanets, selected }) => {
  return <></>;
};

RadarChart.propTypes = {
  selectedPlanets: PropTypes.array,
  selected: PropTypes.bool
};

export default RadarChart;
